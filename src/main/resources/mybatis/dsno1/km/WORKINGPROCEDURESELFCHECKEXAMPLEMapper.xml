<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.km.WORKINGPROCEDURESELFCHECKEXAMPLEMapper">
	
	<!--表名 -->
	<sql id="tableName">
		KM_WORKINGPROCEDURESELFCHECKEXAMPLE
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.WORKINGPROCEDUREEXAMPLE_ID,	
		f.FTESTSTANDARD,	
		f.FTESTITEMNAME,	
		f.FTESTNOTE,	
		f.FISACCORD,	
		f.FRUNNOTE,	
		f.FBACKMAN,	
		f.FBACKTIME,	
		f.WORKINGPROCEDURESELFCHECKEXAMPLE_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		WORKINGPROCEDUREEXAMPLE_ID,	
		FTESTSTANDARD,	
		FTESTITEMNAME,	
		FTESTNOTE,	
		FISACCORD,	
		FRUNNOTE,	
		FBACKMAN,	
		FBACKTIME,	
		WORKINGPROCEDURESELFCHECKEXAMPLE_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{WORKINGPROCEDUREEXAMPLE_ID},	
		#{FTESTSTANDARD},	
		#{FTESTITEMNAME},	
		#{FTESTNOTE},	
		#{FISACCORD},	
		#{FRUNNOTE},	
		#{FBACKMAN},	
		#{FBACKTIME},	
		#{WORKINGPROCEDURESELFCHECKEXAMPLE_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			WORKINGPROCEDURESELFCHECKEXAMPLE_ID = #{WORKINGPROCEDURESELFCHECKEXAMPLE_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			WORKINGPROCEDUREEXAMPLE_ID = #{WORKINGPROCEDUREEXAMPLE_ID},
			FTESTSTANDARD = #{FTESTSTANDARD},
			FTESTITEMNAME = #{FTESTITEMNAME},
			FTESTNOTE = #{FTESTNOTE},
			FISACCORD = #{FISACCORD},
			FRUNNOTE = #{FRUNNOTE},
			FBACKMAN = #{FBACKMAN},
			FBACKTIME = #{FBACKTIME},
			WORKINGPROCEDURESELFCHECKEXAMPLE_ID = WORKINGPROCEDURESELFCHECKEXAMPLE_ID
		where 
			WORKINGPROCEDURESELFCHECKEXAMPLE_ID = #{WORKINGPROCEDURESELFCHECKEXAMPLE_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.WORKINGPROCEDURESELFCHECKEXAMPLE_ID = #{WORKINGPROCEDURESELFCHECKEXAMPLE_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select f1.WPName,
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		left join PP_ProcessWorkOrderExample f1
		on f.WORKINGPROCEDUREEXAMPLE_ID=f1.ProcessWorkOrderExample_ID
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
				
		</if>
		<if test="pd.WORKINGPROCEDUREEXAMPLE_ID != null and pd.WORKINGPROCEDUREEXAMPLE_ID != ''">
			and WORKINGPROCEDUREEXAMPLE_ID = #{pd.WORKINGPROCEDUREEXAMPLE_ID}
		</if>
		<if test="pd.WorkOrderIDRel != null and pd.WorkOrderIDRel != ''">
			and  WORKINGPROCEDUREEXAMPLE_ID in(
			select ProcessWorkOrderExample_ID from PP_ProcessWorkOrderExample where 1=1 and PlanningWorkOrderID=#{pd.WorkOrderIDRel}
		    )
		
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		and WORKINGPROCEDUREEXAMPLE_ID = #{pd.WORKINGPROCEDUREEXAMPLE_ID}
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			WORKINGPROCEDURESELFCHECKEXAMPLE_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	<!--一键修改是否符合 -->
	<update id="editAll" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 		
			FISACCORD = #{FISACCORD},
			FBACKMAN = #{FBACKMAN},
			FBACKTIME = #{FBACKTIME}
		where 
			WORKINGPROCEDUREEXAMPLE_ID = #{WORKINGPROCEDUREEXAMPLE_ID}
	</update>
	<!-- yy356703572qq(彬耶) -->
</mapper>