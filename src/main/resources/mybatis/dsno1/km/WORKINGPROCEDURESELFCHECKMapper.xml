<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.km.WORKINGPROCEDURESELFCHECKMapper">
	
	<!--表名 -->
	<sql id="tableName">
		KM_WORKINGPROCEDURESELFCHECK
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.WORKINGPROCEDURE_ID,	
		f.FTESTSTANDARD,	
		f.FTESTITEMNAME,	
		f.FTESTNOTE,	
		f.WORKINGPROCEDURESELFCHECK_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		WORKINGPROCEDURE_ID,	
		FTESTSTANDARD,	
		FTESTITEMNAME,	
		FTESTNOTE,	
		WORKINGPROCEDURESELFCHECK_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{WORKINGPROCEDURE_ID},	
		#{FTESTSTANDARD},	
		#{FTESTITEMNAME},	
		#{FTESTNOTE},	
		#{WORKINGPROCEDURESELFCHECK_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			WORKINGPROCEDURESELFCHECK_ID = #{WORKINGPROCEDURESELFCHECK_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			WORKINGPROCEDURE_ID = #{WORKINGPROCEDURE_ID},
			FTESTSTANDARD = #{FTESTSTANDARD},
			FTESTITEMNAME = #{FTESTITEMNAME},
			FTESTNOTE = #{FTESTNOTE},
			WORKINGPROCEDURESELFCHECK_ID = WORKINGPROCEDURESELFCHECK_ID
		where 
			WORKINGPROCEDURESELFCHECK_ID = #{WORKINGPROCEDURESELFCHECK_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.WORKINGPROCEDURESELFCHECK_ID = #{WORKINGPROCEDURESELFCHECK_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1 
		<if test="WORKINGPROCEDURE_ID != null and WORKINGPROCEDURE_ID != ''"><!-- 工艺工序id -->
			and f.WORKINGPROCEDURE_ID = #{WORKINGPROCEDURE_ID}
		</if>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			WORKINGPROCEDURESELFCHECK_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- yy356703572qq(彬耶) -->
</mapper>