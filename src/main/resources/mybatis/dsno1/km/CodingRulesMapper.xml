<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.km.CodingRulesMapper">
	
	<!--表名 -->
	<sql id="tableName">
		KM_CODINGRULES
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.CODINGRULESTYPE,	
		f.CODINGRULESNAME,	
		f.SCOPEOFAPPLICATION,	
		f.CODINGRULESDESCRIPTION,	
		f.FSTATUS,	
		f.TERMOFVALIDITY,	
		f.INITIALKEY,	
		f.GETVALUE,	
		f.ACQUISITIONTIME,	
		f.CODINGRULES_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		CODINGRULESTYPE,	
		CODINGRULESNAME,	
		SCOPEOFAPPLICATION,	
		CODINGRULESDESCRIPTION,	
		FSTATUS,	
		TERMOFVALIDITY,	
		INITIALKEY,	
		GETVALUE,	
		ACQUISITIONTIME,	
		CODINGRULES_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{CODINGRULESTYPE},	
		#{CODINGRULESNAME},	
		#{SCOPEOFAPPLICATION},	
		#{CODINGRULESDESCRIPTION},	
		#{FSTATUS},	
		#{TERMOFVALIDITY},	
		#{INITIALKEY},	
		#{GETVALUE},	
		#{ACQUISITIONTIME},	
		#{CODINGRULES_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			CODINGRULES_ID = #{CODINGRULES_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			CODINGRULESTYPE = #{CODINGRULESTYPE},
			CODINGRULESNAME = #{CODINGRULESNAME},
			SCOPEOFAPPLICATION = #{SCOPEOFAPPLICATION},
			CODINGRULESDESCRIPTION = #{CODINGRULESDESCRIPTION},
			FSTATUS = #{FSTATUS},
			TERMOFVALIDITY = #{TERMOFVALIDITY},
			INITIALKEY = #{INITIALKEY},
			GETVALUE = #{GETVALUE},
			ACQUISITIONTIME = #{ACQUISITIONTIME},
			CODINGRULES_ID = CODINGRULES_ID
		where 
			CODINGRULES_ID = #{CODINGRULES_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.CODINGRULES_ID = #{CODINGRULES_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			CODINGRULES_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<select id='listByCondition'  parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="CODINGRULESTYPE != null and CODINGRULESTYPE != ''"><!-- 类型 -->
			and
				(
					f.CODINGRULESTYPE = #{CODINGRULESTYPE}
				)
		</if>
		<if test="CODINGRULESNAME != null and CODINGRULESNAME != ''"><!-- 名称 -->
			and
				(
					f.CODINGRULESNAME = #{CODINGRULESNAME}
				)
		</if>
	</select>
	<select id="getDataByCodingRulesType"  parameterType="pd" resultType="pd">
	
		select 
		
		r.CODINGRULESTYPE,	
		r.CODINGRULESNAME,	
		r.SCOPEOFAPPLICATION,	
		r.CODINGRULESDESCRIPTION,	
		r.FSTATUS,	
		r.TERMOFVALIDITY,	
		r.INITIALKEY,	
		r.GETVALUE,	
		r.ACQUISITIONTIME,	
		r.CODINGRULES_ID,
		d.CODERULEID,
		d.DETAILTYPE,
		d.FLENGTH,
		d.FFORMAT,
		d.SETTINGVALUE,
		d.STARTINGVALUE,
		d.TSTEP,
		d.RESETPERIOD,
		d.STREAMCODING,
		d.FSequence,
		d.CODINGRULESDETAIL_ID
		
		
		
		 from KM_CodingRules r 
		
		left join KM_CodingRulesDetail d on d.CodeRuleID = r.CodingRules_ID
		
		where CODINGRULESTYPE =#{CODINGRULESTYPE} 
		
		order by 	d.FSequence
			
	</select>
	
	
	<!-- yy356703572qq(彬耶) -->
</mapper>