<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.zm.SceneTimeMapper">
	
	<!--表名 -->
	<sql id="tableName">
		ZM_SCENETIME
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.StartTime,	
		f.EndTime,	
		f.Creator,	
		f.CreateTime,	
		f.Modified,	
		f.ModifiedTime,	
		f.Extend1,	
		f.Extend2,	
		f.Extend3,
		f.Scene_ID,
		f.SCENETIME_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		StartTime,	
		EndTime,	
		Creator,	
		CreateTime,	
		Modified,	
		ModifiedTime,	
		Extend1,	
		Extend2,	
		Extend3,
		Scene_ID,
		SCENETIME_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{StartTime},	
		#{EndTime},	
		#{Creator},	
		#{CreateTime},	
		#{Modified},	
		#{ModifiedTime},	
		#{Extend1},	
		#{Extend2},	
		#{Extend3},
		#{Scene_ID},
		#{SCENETIME_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			SCENETIME_ID = #{SCENETIME_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			StartTime = #{StartTime},
			EndTime = #{EndTime},
			Creator = #{Creator},
			CreateTime = #{CreateTime},
			Modified = #{Modified},
			ModifiedTime = #{ModifiedTime},
			Extend1 = #{Extend1},
			Extend2 = #{Extend2},
			Extend3 = #{Extend3},
			SCENETIME_ID = SCENETIME_ID
		where 
			SCENETIME_ID = #{SCENETIME_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.SCENETIME_ID = #{SCENETIME_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		ORDER BY CreateTime ASC
	</select>

	<!-- 列表(全部) -->
	<select id="listAllByDate" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include> f
		where
		Scene_ID = #{Scene_ID}
		and StartTime != 'undefined' and StartTime != '' and StartTime is not null
		and EndTime != 'undefined' and EndTime != '' and EndTime is not null
		and StartTime &lt;= #{TimePrarm}
		and EndTime &gt;= #{TimePrarm}
		ORDER BY CreateTime ASC
	</select>

	<!-- 批量删除 -->
	<delete id="deleteByScene" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where
		Scene_ID = #{SCENE_ID}
	</delete>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			SCENETIME_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- yy356703572qq(彬耶) -->
</mapper>