<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.zm.LoopTimeMapper">
	
	<!--表名 -->
	<sql id="tableName">
		ZM_LOOPTIME
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.StartTime,	
		f.EndTime,	
		f.Creator,	
		f.CreateTime,	
		f.Modified,	
		f.ModifiedTime,	
		f.Extend1,	
		f.Extend2,	
		f.Extend3,
		f.Loop_ID,
		f.LOOPTIME_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		StartTime,	
		EndTime,	
		Creator,	
		CreateTime,	
		Modified,	
		ModifiedTime,	
		Extend1,	
		Extend2,	
		Extend3,
		Loop_ID,
		LOOPTIME_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{StartTime},	
		#{EndTime},	
		#{Creator},	
		#{CreateTime},	
		#{Modified},	
		#{ModifiedTime},	
		#{Extend1},	
		#{Extend2},	
		#{Extend3},
		#{Loop_ID},
		#{LOOPTIME_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			LOOPTIME_ID = #{LOOPTIME_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			StartTime = #{StartTime},
			EndTime = #{EndTime},
			Creator = #{Creator},
			CreateTime = #{CreateTime},
			Modified = #{Modified},
			ModifiedTime = #{ModifiedTime},
			Extend1 = #{Extend1},
			Extend2 = #{Extend2},
			Extend3 = #{Extend3},
			LOOPTIME_ID = LOOPTIME_ID
		where 
			LOOPTIME_ID = #{LOOPTIME_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.LOOPTIME_ID = #{LOOPTIME_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		ORDER BY CreateTime ASC
	</select>

	<!-- 按当前系统时间及回路ID查询该回路下的时间列表(全部) -->
	<select id="listAllByTime" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include> f
		where Loop_ID = #{Loop_ID}
		and StartTime != 'undefined' and StartTime != '' and StartTime is not null
		and EndTime != 'undefined' and EndTime != '' and EndTime is not null
		and StartTime &lt;= #{TimePrarm}
		and EndTime &gt;= #{TimePrarm}
		ORDER BY CreateTime ASC
	</select>

	<!-- 按当前系统时间及回路ID查询该回路下的时间列表(分钟相等) -->
	<select id="listAllByTimeStart" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include> f
		where
		      --Loop_ID = #{Loop_ID} and
		 StartTime != 'undefined' and StartTime != '' and StartTime is not null
		and EndTime != 'undefined' and EndTime != '' and EndTime is not null
-- 		and StartTime = #{TimePrarm}
		ORDER BY CreateTime ASC
	</select>

	<!-- 按当前系统时间及回路ID查询该回路下的时间列表(分钟相等) -->
	<select id="listAllByTimeEnd" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include> f
		where
		      --Loop_ID = #{Loop_ID} and
		 StartTime != 'undefined' and StartTime != '' and StartTime is not null
		and EndTime != 'undefined' and EndTime != '' and EndTime is not null
		--and EndTime = #{TimePrarm}
		ORDER BY CreateTime ASC
	</select>

	<!-- 批量删除 -->
	<delete id="deleteByLoop" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where
		Loop_ID = #{LOOP_ID}
	</delete>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			LOOPTIME_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- yy356703572qq(彬耶) -->
</mapper>