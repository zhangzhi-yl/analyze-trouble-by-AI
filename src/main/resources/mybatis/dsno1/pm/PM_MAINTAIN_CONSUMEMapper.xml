<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.pm.PM_MAINTAIN_CONSUMEMapper">
	
	<!--表名 -->
	<sql id="tableName">
		PM_MAINTAIN_CONSUME
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.PM_MAINTAIN_ID,	
		f.IF_CREATE_PICKING,	
		f.SOURCE,	
		f.MAT_BASIC_ID,	
		f.APPLYFOR_NUM,	
		f.CONSUME_NUM,	
		f.RESERVE_ONE,	
		f.RESERVE_TWO,	
		f.RESERVE_THREE,	
		f.PM_MAINTAIN_CONSUME_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		PM_MAINTAIN_ID,	
		IF_CREATE_PICKING,	
		SOURCE,	
		MAT_BASIC_ID,	
		APPLYFOR_NUM,	
		CONSUME_NUM,	
		RESERVE_ONE,	
		RESERVE_TWO,	
		RESERVE_THREE,	
		PM_MAINTAIN_CONSUME_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{PM_MAINTAIN_ID},	
		#{IF_CREATE_PICKING},	
		#{SOURCE},	
		#{MAT_BASIC_ID},	
		#{APPLYFOR_NUM},	
		#{CONSUME_NUM},	
		#{RESERVE_ONE},	
		#{RESERVE_TWO},	
		#{RESERVE_THREE},	
		#{PM_MAINTAIN_CONSUME_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			PM_MAINTAIN_CONSUME_ID = #{PM_MAINTAIN_CONSUME_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			IF_CREATE_PICKING = #{IF_CREATE_PICKING},
			SOURCE = #{SOURCE},
			MAT_BASIC_ID = #{MAT_BASIC_ID},
			APPLYFOR_NUM = #{APPLYFOR_NUM},
			CONSUME_NUM = #{CONSUME_NUM},
			RESERVE_ONE = #{RESERVE_ONE},
			RESERVE_TWO = #{RESERVE_TWO},
			RESERVE_THREE = #{RESERVE_THREE},
			PM_MAINTAIN_CONSUME_ID = PM_MAINTAIN_CONSUME_ID
		where 
			PM_MAINTAIN_CONSUME_ID = #{PM_MAINTAIN_CONSUME_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>,f1.MAT_NAME AS MAT_BASIC_NAME,f1.MAT_CODE
		from 
		<include refid="tableName"></include> f
		LEFT JOIN MBASE_MAT_BASIC f1 ON f.MAT_BASIC_ID = f1.MAT_BASIC_ID
		where 
			f.PM_MAINTAIN_CONSUME_ID = #{PM_MAINTAIN_CONSUME_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>,f1.MAT_NAME AS MAT_BASIC_NAME,f1.MAT_CODE
		from 
		<include refid="tableName"></include> f
		LEFT JOIN MBASE_MAT_BASIC f1 ON f.MAT_BASIC_ID = f1.MAT_BASIC_ID
		where
			f.PM_MAINTAIN_ID = #{pd.PM_MAINTAIN_ID}
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
					f.SOURCE LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					f1.MAT_NAME LIKE '%'+ #{pd.KEYWORDS}+'%' 
					or 
					f.IF_CREATE_PICKING LIKE '%'+ #{pd.KEYWORDS}+'%'
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			PM_MAINTAIN_CONSUME_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- yy356703572qq(彬耶) -->
</mapper>