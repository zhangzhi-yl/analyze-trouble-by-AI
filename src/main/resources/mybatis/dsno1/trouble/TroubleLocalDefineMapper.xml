<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.trouble.TroubleLocalDefineMapper">
	
	<!--表名 -->
	<sql id="tableName">
		HSE_TROUBLELOCALDEFINE
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.LOCAL_ID,	
		f.DEFINE_ID,	
		f.TROUBLELOCALDEFINE_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		LOCAL_ID,	
		DEFINE_ID,	
		TROUBLELOCALDEFINE_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{LOCAL_ID},	
		#{DEFINE_ID},	
		#{TROUBLELOCALDEFINE_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			TROUBLELOCALDEFINE_ID = #{TROUBLELOCALDEFINE_ID}
	</delete>

	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			LOCAL_ID = #{LOCAL_ID},
			DEFINE_ID = #{DEFINE_ID},
			TROUBLELOCALDEFINE_ID = TROUBLELOCALDEFINE_ID
		where 
			TROUBLELOCALDEFINE_ID = #{TROUBLELOCALDEFINE_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.TROUBLELOCALDEFINE_ID = #{TROUBLELOCALDEFINE_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>,g.FNAME as DEFINE_NAME
		from 
		<include refid="tableName"></include> f
		left join HSE_TROUBLEDEFINE g on f.DEFINE_ID = g.TROUBLEDEFINE_ID
		where 1=1
		 <if test="LOCAL_ID != null and LOCAL_ID != ''">
			and  f.LOCAL_ID = #{LOCAL_ID}
		 </if>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			TROUBLELOCALDEFINE_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	<delete id="deleteByLocalId" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		LOCAL_ID = #{LOCAL_ID}
	</delete>
	<!-- yy356703572qq(彬耶) -->
</mapper>