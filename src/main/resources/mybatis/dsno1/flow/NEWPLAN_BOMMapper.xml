<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-mybatis.orgDTD Mapper 3.0EN" "http:mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.flow.NEWPLAN_BOMMapper">

	<!--表名 -->
	<sql id="tableName">
		AT_NEWPLAN_BOM_FLOW
	</sql>

	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>

	<!-- 字段 -->
	<sql id="Field">
		f.NODE_ID,
		f.NODE_NAME,
		f.NODE_KIND,
		f.NODE_TYPE,
		f.PHASE_TYPE,
		f.JUMP_CONDITION,
		f.BEGIN_NODE,
		f.END_NODE,
		f.FCREATOR,
		f.CREATE_TIME,
		f.LAST_MODIFIER,
		f.LAST_MODIFIED_TIME,
		f.FDES,
		f.MASTER_PLAN_FLOW_ID,
		f.SUBPLAN_ID,
		f.RUN_O_ID,
		f.MASTERPLAN_ID,
		f.EXECUTE_STATE,
		f.NEWPLAN_BOM_FLOW
	</sql>

	<!-- 字段用于新增 -->
	<sql id="Field2">
		NODE_ID,
		NODE_NAME,
		NODE_KIND,
		NODE_TYPE,
		PHASE_TYPE,
		JUMP_CONDITION,
		BEGIN_NODE,
		END_NODE,
		FCREATOR,
		CREATE_TIME,
		LAST_MODIFIER,
		LAST_MODIFIED_TIME,
		FDES,
		MASTER_PLAN_FLOW_ID,
		SUBPLAN_ID,
		RUN_O_ID,
		MASTERPLAN_ID,
		EXECUTE_STATE,
		NEWPLAN_BOM_FLOW
	</sql>

	<!-- 字段值 -->
	<sql id="FieldValue">
		#{NODE_ID},
		#{NODE_NAME},
		#{NODE_KIND},
		#{NODE_TYPE},
		#{PHASE_TYPE},
		#{JUMP_CONDITION},
		#{BEGIN_NODE},
		#{END_NODE},
		#{FCREATOR},
		#{CREATE_TIME},
		#{LAST_MODIFIER},
		#{LAST_MODIFIED_TIME},
		#{FDES},
		#{MASTER_PLAN_FLOW_ID},
		#{SUBPLAN_ID},
		#{RUN_O_ID},
		#{MASTERPLAN_ID},
		#{EXECUTE_STATE},
		#{NEWPLAN_BOM_FLOW}
	</sql>

	<!-- 新增 -->
	<insert id="save" parameterType="pd">
		insert into
		<include refid="tableName"></include>
		(
		<include refid="Field2"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>

	<!-- 删除 -->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		NEWPLAN_BOM_ID = #{NEWPLAN_BOM_ID}
	</delete>

	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set
		NODE_ID = #{NODE_ID},
		NODE_NAME = #{NODE_NAME},
		NODE_KIND =
		#{NODE_KIND},
		NODE_TYPE = #{NODE_TYPE},
		PHASE_TYPE = #{PHASE_TYPE},
		JUMP_CONDITION = #{JUMP_CONDITION},
		BEGIN_NODE = #{BEGIN_NODE},
		END_NODE = #{END_NODE},
		FCREATOR = #{FCREATOR},
		CREATE_TIME =
		#{CREATE_TIME},
		LAST_MODIFIER = #{LAST_MODIFIER},
		LAST_MODIFIED_TIME =
		#{LAST_MODIFIED_TIME},
		FDES = #{FDES},
		MASTER_PLAN_FLOW_ID =
		#{MASTER_PLAN_FLOW_ID},
		SUBPLAN_ID = #{SUBPLAN_ID},
		RUN_O_ID =
		#{RUN_O_ID},
		MASTERPLAN_ID = #{MASTERPLAN_ID},
		EXECUTE_STATE =
		#{EXECUTE_STATE},
		NEWPLAN_BOM_FLOW = NEWPLAN_BOM_FLOW
		where
		NEWPLAN_BOM_FLOW = #{NEWPLAN_BOM_FLOW}
	</update>

	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		f
		where
		f.NEWPLAN_BOM_ID = #{NEWPLAN_BOM_ID}
	</select>

	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
			(
			<!-- 根据需求自己加检索条件 字段1 LIKE '%'+ #{pd.KEYWORDS}+'%' or 字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' -->
			)
		</if>
	</select>

	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		f
		where 1=1
		<if test="NODE_ID != null and NODE_ID != ''">
			and f.NODE_ID = #{NODE_ID}
		</if>
		<if test="RUN_O_ID != null and RUN_O_ID != ''">
			and f.RUN_O_ID = #{RUN_O_ID}
		</if>
	</select>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where
		NEWPLAN_BOM_FLOW in
		<foreach item="item" index="index" collection="array" open="("
			separator="," close=")">
			#{item}
		</foreach>
	</delete>
	<delete id="deleteBySubPlanID" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where SUBPLAN_ID = #{SUBPLAN_ID}
	</delete>
	<select id="nextNodeList" parameterType="pd" resultType="pd">

		SELECT
		NODE_ID,SUBPLAN_ID,NODE_TYPE
		FROM
		AT_NEWPLAN_BOM_FLOW
		WHERE
		NODE_ID IN (
		SELECT
		END_NODE
		FROM
		AT_NEWPLAN_BOM_FLOW
		WHERE
		BEGIN_NODE = #{BEGIN_NODE}
		)
		AND SUBPLAN_ID = #{SUBPLAN_ID}
		AND MASTERPLAN_ID = #{MASTERPLAN_ID}


	</select>
	<select id="lastNodeList" parameterType="pd" resultType="pd">

		SELECT
		 *
		FROM
		AT_NEWPLAN_BOM_FLOW
		WHERE
		NODE_ID IN (
		SELECT
		BEGIN_NODE
		FROM
		AT_NEWPLAN_BOM_FLOW
		WHERE
		NODE_KIND = 'line'
		AND END_NODE = #{END_NODE}
		)
		AND SUBPLAN_ID=#{SUBPLAN_ID}
		AND MASTERPLAN_ID = #{MASTERPLAN_ID}

	</select>

	<delete id="deleteBySubPlanIdAndNodeId" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 1=1
		and SUBPLAN_ID = #{SUBPLAN_ID}
		and NODE_ID = #{NODE_ID}
	</delete>
	
	<!-- yy356703572qq(彬耶) -->
</mapper>