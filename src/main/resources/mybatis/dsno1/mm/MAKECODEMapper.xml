<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.mm.MAKECODEMapper">
	
	<!--表名 -->
	<sql id="tableName">
		MM_MAKECODE
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.Ftype,	
		f.FCode,	
		f.FCreatetime,	
		f.FCreator,	
		f.MAKECODE_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		Ftype,	
		FCode,	
		FCreatetime,	
		FCreator,	
		MAKECODE_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{Ftype},	
		#{FCode},	
		#{FCreatetime},	
		#{FCreator},	
		#{MAKECODE_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			MAKECODE_ID = #{MAKECODE_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			Ftype = #{Ftype},
			FCode = #{FCode},
			FCreatetime = #{FCreatetime},
			FCreator = #{FCreator},
			MAKECODE_ID = MAKECODE_ID
		where 
			MAKECODE_ID = #{MAKECODE_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.MAKECODE_ID = #{MAKECODE_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			MAKECODE_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	<!-- 根据物料类型获取当前码 -->
	<select id="getCode" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.Ftype = #{Ftype}
	</select>
	
	<!-- 根据物料类型修改当前码 -->
	<update id="editCode" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			FCode = #{FCode},
			FCreatetime = #{FCreatetime},
			FCreator = #{FCreator}
		where 
			Ftype = #{Ftype}
	</update>
	<!-- yy356703572qq(彬耶) -->
</mapper>