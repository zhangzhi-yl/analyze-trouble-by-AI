<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.project.manager.PROJECTMANAGERVIEWMapper">
	
	<!--表名 -->
	<sql id="tableName">
		DT_PROJECTMANAGERVIEW
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.FTYPENAME,	
		f.PLAN_START_TIME,	
		f.PLAN_END_TIME,	
		f.REAL_START_TIME,	
		f.REAL_END_TIME,	
		f.PROJECT_ID,	
		f.FSORT,
		f.PROGRESS,
		f.PROJECTMANAGERVIEW_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		FTYPENAME,	
		PLAN_START_TIME,	
		PLAN_END_TIME,	
		REAL_START_TIME,	
		REAL_END_TIME,	
		PROJECT_ID,	
		FSORT,
		PROGRESS,
		PROJECTMANAGERVIEW_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{FTYPENAME},	
		#{PLAN_START_TIME},	
		#{PLAN_END_TIME},	
		#{REAL_START_TIME},	
		#{REAL_END_TIME},	
		#{PROJECT_ID},	
		#{FSORT},
		#{PROGRESS},
		#{PROJECTMANAGERVIEW_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			PROJECTMANAGERVIEW_ID = #{PROJECTMANAGERVIEW_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			PLAN_START_TIME = #{PLAN_START_TIME},	
			PLAN_END_TIME = #{PLAN_END_TIME},
			REAL_START_TIME = #{REAL_START_TIME},
			REAL_END_TIME = #{REAL_END_TIME},
			PROJECTMANAGERVIEW_ID = PROJECTMANAGERVIEW_ID
		where 
			PROJECTMANAGERVIEW_ID = #{PROJECTMANAGERVIEW_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.PROJECTMANAGERVIEW_ID = #{PROJECTMANAGERVIEW_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		WHERE PROJECT_ID = #{PROJECT_ID}
		ORDER BY CAST(FSORT AS INT)
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			PROJECTMANAGERVIEW_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- 甘特列表(全部) -->
	<select id="listGanTe" parameterType="pd" resultType="pd">
		SELECT
		f.FTYPENAME text,	
		CONVERT(VARCHAR(100),CONVERT(datetime, f.PLAN_START_TIME, 20),105) +' 00:00:00' start_date,
		CONVERT(VARCHAR(100),CONVERT(datetime, f.PLAN_END_TIME,20),105) +' 23:59:59' end_date,
		ISNULL(f.PROGRESS, '0') progress,
		f.PROJECTMANAGERVIEW_ID id
		from 
		<include refid="tableName"></include> f
		WHERE PROJECT_ID = #{PROJECT_ID}
		ORDER BY CAST(FSORT AS INT)
	</select>
	
	<!-- 依据甘特图返回数据修改 -->
	<update id="editGanTE" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			PLAN_START_TIME = CONVERT(VARCHAR(100),CONVERT(datetime,#{PlannedBeginTime},20), 23),	
			PLAN_END_TIME = CONVERT(VARCHAR(100),CONVERT(datetime,#{PlannedEndTime},20), 23),
			PROGRESS = #{progress}
		where 
			PROJECTMANAGERVIEW_ID = #{PlanningWorkOrder_ID}
	</update>

	<select id="findByName" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.PROJECT_ID = #{PROJECT_ID} and f.FTYPENAME = #{FTYPENAME}
	</select>
</mapper>