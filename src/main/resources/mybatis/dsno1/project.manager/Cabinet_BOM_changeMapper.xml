<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.project.manager.Cabinet_BOM_changeMapper">
	
	<!--表名 -->
	<sql id="tableName">
		PMS_CABINET_BOM_CHANGE
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.CABINET_BOM_ID,	
		f.MAT_BASIC_ID,	
		f.MAT_NAME,	
		f.MAT_CODE,	
		f.MAT_CLASS,	
		f.MAT_SPECS,	
		f.MAT_MAIN_UNIT,	
		f.MAT_BRAND,	
		f.BOM_COUNT,	
		f.UP_LIMIT,	
		f.DOWM_LIMIT,	
		f.MAT_CATEGRAY,	
		f.Cabinet_Assembly_Detail_ID,	
		f.FSTATE,	
		f.REMARK,	
		f.If_Purchase,	
		f.FAudit_State,	
		f.Change_Duty,	
		f.FORDER,	
		f.CG_COUNT,	
		f.FVersion,	
		f.FChange
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		CABINET_BOM_ID,	
		MAT_BASIC_ID,	
		MAT_NAME,	
		MAT_CODE,	
		MAT_CLASS,	
		MAT_SPECS,	
		MAT_MAIN_UNIT,	
		MAT_BRAND,	
		BOM_COUNT,	
		UP_LIMIT,	
		DOWM_LIMIT,	
		MAT_CATEGRAY,	
		Cabinet_Assembly_Detail_ID,	
		FSTATE,	
		REMARK,	
		If_Purchase,	
		FAudit_State,	
		Change_Duty,	
		FORDER,	
		CG_COUNT,	
		FVersion,	
		FChange
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{CABINET_BOM_ID},	
		#{MAT_BASIC_ID},	
		#{MAT_NAME},	
		#{MAT_CODE},	
		#{MAT_CLASS},	
		#{MAT_SPECS},	
		#{MAT_MAIN_UNIT},	
		#{MAT_BRAND},	
		#{BOM_COUNT},	
		#{UP_LIMIT},	
		#{DOWM_LIMIT},	
		#{MAT_CATEGRAY},	
		#{Cabinet_Assembly_Detail_ID},	
		#{FSTATE},	
		#{REMARK},	
		#{If_Purchase},	
		#{FAudit_State},	
		#{Change_Duty},	
		#{FORDER},	
		#{CG_COUNT},	
		#{FVersion},	
		#{FChange}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into PMS_Cabinet_BOM_change select * from PMS_Cabinet_BOM where Cabinet_Assembly_Detail_ID=#{Cabinet_Assembly_Detail_ID}
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			Cabinet_Assembly_Detail_ID = #{Cabinet_Assembly_Detail_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			CABINET_BOM_ID = #{CABINET_BOM_ID},
			MAT_BASIC_ID = #{MAT_BASIC_ID},
			MAT_NAME = #{MAT_NAME},
			MAT_CODE = #{MAT_CODE},
			MAT_CLASS = #{MAT_CLASS},
			MAT_SPECS = #{MAT_SPECS},
			MAT_MAIN_UNIT = #{MAT_MAIN_UNIT},
			MAT_BRAND = #{MAT_BRAND},
			BOM_COUNT = #{BOM_COUNT},
			UP_LIMIT = #{UP_LIMIT},
			DOWM_LIMIT = #{DOWM_LIMIT},
			MAT_CATEGRAY = #{MAT_CATEGRAY},
			Cabinet_Assembly_Detail_ID = #{Cabinet_Assembly_Detail_ID},
			FSTATE = #{FSTATE},
			REMARK = #{REMARK},
			If_Purchase = #{If_Purchase},
			FAudit_State = #{FAudit_State},
			Change_Duty = #{Change_Duty},
			FORDER = #{FORDER},
			CG_COUNT = #{CG_COUNT},
			FVersion = #{FVersion},
			FChange = #{FChange}
		where 
			Cabinet_BOM_change_ID = #{Cabinet_BOM_change_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.Cabinet_BOM_change_ID = #{Cabinet_BOM_change_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					字段2 LIKE '%'+ #{pd.KEYWORDS}+'%' 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where f.Cabinet_Assembly_Detail_ID = #{Cabinet_Assembly_Detail_ID}
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			Cabinet_BOM_change_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- yy356703572qq(彬耶) -->
</mapper>