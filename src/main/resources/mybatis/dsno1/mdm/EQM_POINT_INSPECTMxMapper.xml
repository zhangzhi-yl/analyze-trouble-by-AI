<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.mdm.EQM_POINT_INSPECTMxMapper">
	
	<!--表名 -->
	<sql id="tableName">
		MDM_EQM_POINT_INSPECTMX
	</sql>

	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.FPLACE,	
		f.FWATCHDOG,	
		f.FINSPECTION_TIME,	
		f.FINSPECTION_RESULT,	
		f.FINSPECTION_PROJECT,	
		f.FPROJECT_FDS,	
		f.FCREATOR,	
		f.CREATE_TIME,	
		f.EQM_POINT_INSPECTMX_ID,
		f.EQM_POINT_INSPECT_ID
	</sql>
	
	<!-- 字段 -->
	<sql id="Field2">
		FPLACE,	
		FWATCHDOG,	
		FINSPECTION_TIME,	
		FINSPECTION_RESULT,	
		FINSPECTION_PROJECT,	
		FPROJECT_FDS,	
		FCREATOR,	
		CREATE_TIME,	
		EQM_POINT_INSPECTMX_ID,
		EQM_POINT_INSPECT_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{FPLACE},	
		#{FWATCHDOG},	
		#{FINSPECTION_TIME},	
		#{FINSPECTION_RESULT},	
		#{FINSPECTION_PROJECT},	
		#{FPROJECT_FDS},	
		#{FCREATOR},	
		#{CREATE_TIME},	
		#{EQM_POINT_INSPECTMX_ID},
		#{EQM_POINT_INSPECT_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			EQM_POINT_INSPECTMX_ID = #{EQM_POINT_INSPECTMX_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			FPLACE = #{FPLACE},
			FWATCHDOG = #{FWATCHDOG},
			FINSPECTION_TIME = #{FINSPECTION_TIME},
			FINSPECTION_RESULT = #{FINSPECTION_RESULT},
			FINSPECTION_PROJECT = #{FINSPECTION_PROJECT},
			FPROJECT_FDS = #{FPROJECT_FDS},
			FCREATOR = #{FCREATOR},
			CREATE_TIME = #{CREATE_TIME},
			EQM_POINT_INSPECT_ID = #{EQM_POINT_INSPECT_ID},
			EQM_POINT_INSPECTMX_ID = EQM_POINT_INSPECTMX_ID
		where 
			EQM_POINT_INSPECTMX_ID = #{EQM_POINT_INSPECTMX_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.EQM_POINT_INSPECTMX_ID = #{EQM_POINT_INSPECTMX_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where
			f.EQM_POINT_INSPECT_ID = #{pd.EQM_POINT_INSPECT_ID}
		<if test="pd.KEYWORDS!= null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
					f.FPLACE LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					f.FWATCHDOG LIKE '%'+ #{pd.KEYWORDS}+'%' 
					or 
					f.FINSPECTION_PROJECT LIKE '%'+ #{pd.KEYWORDS}+'%'
				)
		</if>
		[fhstart] order by f.CREATE_TIME DESC [fhend] 
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			EQM_POINT_INSPECTMX_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- 查询明细总数 -->
	<select id="findCount" parameterType="pd" resultType="pd">
		select
			count(*) zs
		from 
			<include refid="tableName"></include>
		where
			EQM_POINT_INSPECT_ID = #{EQM_POINT_INSPECT_ID}
	</select>
		<!-- YY 356703572 -->
</mapper>