<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.yy.mapper.dsno1.mom.POLLINGMapper">
	
	<!--表名 -->
	<sql id="tableName">
		MOM_POLLING
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.POLLING_NUMBER,	
		f.POLLING_DATE,	
		f.POLLING_STATE,	
		f.POLLTOR,	
		f.POLLTIME,	
		f.DOCUMENTTEMPLATE_ID,	
		f.FSTENCIL_NAME,	
		f.TEMBILL_EXECUTE_ID,	
		f.FCREATOR,	
		f.CREATE_TIME,	
		f.FEXTEND1,	
		f.FEXTEND2,	
		f.FEXTEND3,	
		f.FEXTEND4,	
		f.FEXTEND5,	
		f.FEFFECT,	
		f.FDS,	
		f.POLLING_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		POLLING_NUMBER,	
		POLLING_DATE,	
		POLLING_STATE,	
		POLLTOR,	
		POLLTIME,	
		DOCUMENTTEMPLATE_ID,	
		FSTENCIL_NAME,	
		TEMBILL_EXECUTE_ID,	
		FCREATOR,	
		CREATE_TIME,	
		FEXTEND1,	
		FEXTEND2,	
		FEXTEND3,	
		FEXTEND4,	
		FEXTEND5,	
		FEFFECT,	
		FDS,	
		POLLING_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{POLLING_NUMBER},	
		#{POLLING_DATE},	
		#{POLLING_STATE},	
		#{POLLTOR},	
		#{POLLTIME},	
		#{DOCUMENTTEMPLATE_ID},	
		#{FSTENCIL_NAME},	
		#{TEMBILL_EXECUTE_ID},	
		#{FCREATOR},	
		#{CREATE_TIME},	
		#{FEXTEND1},	
		#{FEXTEND2},	
		#{FEXTEND3},	
		#{FEXTEND4},	
		#{FEXTEND5},	
		#{FEFFECT},	
		#{FDS},	
		#{POLLING_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			POLLING_ID = #{POLLING_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			POLLING_NUMBER = #{POLLING_NUMBER},
			POLLING_DATE = #{POLLING_DATE},
			POLLING_STATE = #{POLLING_STATE},
			POLLTOR = #{POLLTOR},
			POLLTIME = #{POLLTIME},
			DOCUMENTTEMPLATE_ID = #{DOCUMENTTEMPLATE_ID},
			FSTENCIL_NAME = #{FSTENCIL_NAME},
			TEMBILL_EXECUTE_ID = #{TEMBILL_EXECUTE_ID},
			FCREATOR = #{FCREATOR},
			CREATE_TIME = #{CREATE_TIME},
			FEXTEND1 = #{FEXTEND1},
			FEXTEND2 = #{FEXTEND2},
			FEXTEND3 = #{FEXTEND3},
			FEXTEND4 = #{FEXTEND4},
			FEXTEND5 = #{FEXTEND5},
			FEFFECT = #{FEFFECT},
			FDS = #{FDS},
			POLLING_ID = POLLING_ID
		where 
			POLLING_ID = #{POLLING_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.POLLING_ID = #{POLLING_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.KEYWORDS != null and pd.KEYWORDS != ''"><!-- 关键词检索 -->
			and
				(
					f.POLLING_NUMBER LIKE '%'+ #{pd.KEYWORDS}+'%'
					 or 
					f.POLLING_STATE LIKE '%'+ #{pd.KEYWORDS}+'%' 
				)
		</if>
		[fhstart] order by f.CREATE_TIME DESC [fhend]
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			POLLING_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	
	<!-- 修改检验状态 -->
	<update id="editState" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			POLLING_STATE = #{POLLING_STATE},
			FEFFECT = #{FEFFECT}
		where 
			POLLING_ID = #{POLLING_ID}
	</update>
	<!-- YY 356703572 -->
</mapper>